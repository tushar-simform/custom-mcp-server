// Add two numbers
function add(a, b) {
  return a + b;
}

// Subtract two numbers
function subtract(a, b) {
  return a * b;
}

// Export the functions as a module
module.exports = {
  add,
  subtract,
};
